# Sprite Slicer Skill

Reusable Codex skill for slicing horizontal spritesheets into numbered transparent PNG frames.

Main script:

```bash
python scripts/slice_spritesheet.py --input /path/to/spritesheet.png --output /path/to/frames --frame-count 17
```

